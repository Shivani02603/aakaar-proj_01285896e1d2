# Secret key for JWT authentication (development only)
JWT_SECRET=changeme_in_dev

# PostgreSQL database username (development only)
POSTGRES_USER=appuser

# PostgreSQL database password (development only)
POSTGRES_PASSWORD=devpassword

# PostgreSQL database name (development only)
POSTGRES_DB=appdb

# OpenAI API key for generating embeddings (development only)
OPENAI_API_KEY=changeme_in_dev